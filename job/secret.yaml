# ⚠️  TEMPLATE ONLY - DO NOT EDIT THIS FILE WITH REAL CREDENTIALS
#
# This file is a reference template showing the secret structure.
# DO NOT commit actual credentials to git!
#
# To create the secret, use:
#
#   oc create secret generic minio-credentials \
#     --from-literal=MINIO_ENDPOINT=https://your-minio-endpoint \
#     --from-literal=MINIO_ACCESS_KEY=your_access_key \
#     --from-literal=MINIO_SECRET_KEY=your_secret_key \
#     --from-literal=MINIO_BUCKET=kb-documents \
#     --from-literal=MINIO_PREFIX=data/ \
#     -n servicenow-ai-poc
#
---
apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
type: Opaque
stringData:
  MINIO_ENDPOINT: "CHANGE_ME"        # e.g., https://minio-api.example.com
  MINIO_ACCESS_KEY: "CHANGE_ME"
  MINIO_SECRET_KEY: "CHANGE_ME"
  MINIO_BUCKET: "kb-documents"       # default bucket name
  MINIO_PREFIX: "data/"               # default prefix
